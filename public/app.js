const $=s=>document.querySelector(s);let charts={};
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);const num=n=>new Intl.NumberFormat('en-US').format(n);
const colors=['#1769e8','#10a98d','#f3a91b','#805ad5'];
function baseOptions(){return{responsive:true,maintainAspectRatio:false,plugins:{legend:{labels:{usePointStyle:true,padding:16,color:'#536174',font:{size:10}}}},scales:{x:{grid:{display:false},ticks:{color:'#718096',font:{size:10}}},y:{grid:{color:'#e9eef5'},ticks:{color:'#718096',font:{size:10},callback:v=>'$'+(v>=1000?(v/1000)+'k':v)}}}}}
function draw(d){Object.values(charts).forEach(c=>c.destroy());const labels=d.trend.map(x=>x.month), cats=d.categories.map(x=>x.name);charts.line=new Chart($('#line'),{type:'line',data:{labels,datasets:[{label:'Revenue',data:d.trend.map(x=>x.revenue),borderColor:colors[0],backgroundColor:'#1769e817',fill:true,tension:.35,borderWidth:3,pointRadius:3,pointBackgroundColor:'#fff',pointBorderWidth:2}]},options:baseOptions()});charts.bar=new Chart($('#bar'),{type:'bar',data:{labels:cats,datasets:[{label:'Revenue',data:d.categories.map(x=>x.revenue),backgroundColor:colors,borderRadius:6,barThickness:28}]},options:baseOptions()});charts.donut=new Chart($('#donut'),{type:'doughnut',data:{labels:cats,datasets:[{data:d.categories.map(x=>x.revenue),backgroundColor:colors,borderWidth:0,hoverOffset:7}]},options:{responsive:true,maintainAspectRatio:false,cutout:'68%',plugins:{legend:{position:'bottom',labels:{usePointStyle:true,padding:14,color:'#536174',font:{size:9}}}}}});$('#revenue').textContent=money(d.stats.revenue);$('#orders').textContent=num(d.stats.orders);$('#aov').textContent=money(d.stats.avgOrderValue);$('#top').textContent=d.stats.topCategory;const g=$('#growth');g.textContent=`${d.stats.growth>=0?'+':''}${d.stats.growth.toFixed(1)}% vs previous month`;g.className=d.stats.growth>=0?'positive':'negative';$('#count').textContent=`${d.recent.length} records`;$('#rows').innerHTML=d.recent.map(r=>`<tr><td>${new Date(r.date+'T00:00:00').toLocaleDateString('en-US',{month:'short',day:'2-digit',year:'numeric'})}</td><td><span class="pill">${r.category}</span></td><td>${r.orders}</td><td class="rev">${money(r.revenue)}</td></tr>`).join('')}
async function load(){const m=$('#message');m.style.display='none';$('#apply').disabled=true;$('#apply').textContent='Loading…';try{const q=new URLSearchParams({from:$('#from').value,to:$('#to').value,category:$('#category').value});const r=await fetch('/api/dashboard?'+q);if(!r.ok)throw new Error();draw(await r.json())}catch(e){m.textContent='Could not load the dashboard data. Please make sure the backend is running.';m.style.display='block'}finally{$('#apply').disabled=false;$('#apply').textContent='Apply filters'}}$('#filters').addEventListener('submit',e=>{e.preventDefault();load()});load();
